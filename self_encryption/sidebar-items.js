initSidebarItems({"struct":[["SelfEncryptor","This is the encryption object and all file handling should be done via this as the low level mechanism to read and write *content* this library has no knowledge of file metadata. This is a library to ensure content is secured"]],"mod":[["datamap","Information required to recover file content from chunks"]],"constant":[["MAX_CHUNK_SIZE","MAX_CHUNK_SIZE defined as 1MB"],["MIN_CHUNK_SIZE","MIN_CHUNK_SIZE defined as 1KB"]],"fn":[["create_temp_dir","Will use a tempdir to stream un procesed data, although this is done vie AES streaming with a random key and IV"],["xor","Helper function to XOR a data with a pad (pad will be rotated to fill the length) ### Usage    let data = vec![1u8, 2u8, 3u8, 4u8, 5u8];    let pad = vec![8u8, 7u8];    let xor_result = xor(&data, &pad);"]],"trait":[["Storage","Storage traits of SelfEncryptor data stored in Storage is encrypted, name is the SHA512 hash of content Storage can be in-memory HashMap or disk based"]]});